enum SectionID {
  filmsList = '#filmsList',
  favorites = '#favorites',
  search = '#search',
  header = '#header',
  content = '#content',
  responseError = '#responseError',
}

export default SectionID;
