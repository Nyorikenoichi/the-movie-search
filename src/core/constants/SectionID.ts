enum SectionID {
  filmsList = '#filmsList',
  favorites = '#favorites',
  search = '#search-wrapper',
  header = '#header',
  content = '#content',
  footer = '#footer',
  responseError = '#responseError',
}

export default SectionID;
